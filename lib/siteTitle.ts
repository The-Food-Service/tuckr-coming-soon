/** Default browser tab title (keep in sync with `metadata.title` in app/layout.tsx). */
export const SITE_DOCUMENT_TITLE = "Tuckr | Pre-order campus food. Skip the queue.";

/** Shown in rotation when the user switches to another tab (after they have viewed this one). */
export const TAB_ATTENTION_TITLES = [
  "Tuckr | Still here?",
  "Pre-order campus food",
  "Skip the queue with Tuckr",
  "Tuckr | Come back when you can",
  "Campus pick & go",
] as const;
